[project]
name = "blogfirst"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Amirreza", email = "itsvilliyam@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
]

[project.scripts]
blogfirst = "blogfirst:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
